# pseudo-lang
typescript lang pseudo


bruteforce any language write psuedo code

```ts
// %WebTransportBidirectionalStream new prototype () {
    // MAIN MENU OPT_
    // TABLE _DATA_ INSERTED HERE
    // exfil __team.txt__
//}
// sub command_&requestAnimationFrame{...}
    // sub command_&requestAnimationFrame{ 'ongoing' }
    // function parameter requestAnimationFrame():any
        // use Text.ext
        // our public @keywords{
            // my !!command private class ()
            // bless $keyword.list[ 0 [1] 2 3 ]
            // :select: + '!!@$%&*Keywords_sigils_0-9'
            // for my next command $$
    //*Permissions={...@system...system online!}
    
    //import system from sources.pl
    //export SourceBufferList.txt
        // open source - all rights reserved 2024
        //} END - EOL

```
```
// import SYS from: source/route/path: $File
// use utf8;
// use warnings;
// use strict;

// my $File = /runtime/file.ext
// a( parameter ) -> select index > connect|map

// BEGIN:
//     For Example:

// _Title_ == _Main_

// for while-do command $$command
// verify( login|password > auth() )

// My Sample: _complexity_ vs _simpicity_
// my _simpicity_ public void[$1 >goto: $2]
// our _simpicity_ private class[ opt src code]
// bless _simpicity_ restricted dir[runtime/file.ext*]

// return 0;
// return 1;

// END;
```
